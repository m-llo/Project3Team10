import React from 'react';
i

const LoginButton = () => {
  return (
 
      <button
      variant="outline-info"
      onClick={}>
        Log In
      </button>
    )
  }

export default LoginButton